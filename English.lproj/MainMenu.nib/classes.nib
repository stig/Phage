{
    IBClasses = (
        {CLASS = BoardView; LANGUAGE = ObjC; SUPERCLASS = NSMatrix; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {newGame = id; undo = id; }; 
            CLASS = Phage; 
            LANGUAGE = ObjC; 
            OUTLETS = {blackMoves = NSTableView; board = BoardView; whiteMoves = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}